# macrobot

## Local commands
```bash
#  gen `worker-configuration.d.ts`
bunx wrangler types

# discord bot admin commands
npm run cmd:fetch-all
npm run cmd:register -- [cmd]
npm run cmd:register-all
npm run cmd:delete -- [cmd]
npm run cmd:test-genai
npm run cmd run/fetch-all.ts
```
